APP_NAME="CRM"
APP_ENV=local
APP_DEBUG=true

# Local development talks to the MySQL container through the published port:
# docker compose up -d mysql
DB_HOST=127.0.0.1
DB_PORT=33064
DB_NAME=crm
DB_USER=crm
DB_PASS=secret
