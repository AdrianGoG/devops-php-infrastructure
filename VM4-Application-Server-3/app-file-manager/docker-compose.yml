services:

  nginx:
    image: nginx:latest
    container_name: filemgr-nginx

    ports:
      - "8082:80"

    volumes:
      - ./src:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf

    depends_on:
      - php

  php:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: filemgr-php
    volumes:
      - ./src:/var/www/html
      - uploads:/var/www/html/storage/app/uploads

    depends_on:
      - mysql

  mysql:
    image: mysql:8.0
    container_name: filemgr-mysql
    restart: unless-stopped

    environment:
      MYSQL_DATABASE: filemanager
      MYSQL_USER: filemanager
      MYSQL_PASSWORD: secret
      MYSQL_ROOT_PASSWORD: root

    ports:
      - "33068:3306"

    volumes:
      - mysql_data:/var/lib/mysql
      - ./docker/mysql/init:/docker-entrypoint-initdb.d:ro

volumes:
  mysql_data:
  uploads:
